import {StyleSheet} from 'react-native';

export const GlobalStyles = StyleSheet.create({
  containerCenter: {
    alignItems: 'center',
    justifyContent: 'center',
  },
  marginHorinzontal: {
    marginHorizontal: 15,
  },
});
